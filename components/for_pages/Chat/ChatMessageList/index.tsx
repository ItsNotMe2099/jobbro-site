import styles from './index.module.scss'
import Card from '@/components/for_pages/Common/Card'

interface Props {

}

export const ChatMessageList = (props: Props) => {
  return (
    <div className={styles.root}>
      <Card>
        
      </Card>
    </div>
  )
}